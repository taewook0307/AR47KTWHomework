#include <iostream>
#include <vector>
#include <string>

int Function()
{
    return 10;
}

int main()
{
    // 대입할 수 있고 메모리 영역을 특정할 수 있는 메모리들 : LValue
    // 사용하거나 참조할 수만 있는 메모리들 : RValue
    // LValue는 RValue가 될 수 있음
    // RValue는 LValue가 될 수 없음

    // & : LValue Reference => 명확하게 위치가 있고 메모리를 특정할 수 있어야 함
    // && : RValue Reference => RValue를 받을 수 있음


}